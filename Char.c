#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	char Kien[100];
	ptintf("hey");
	gets(Kien);
	printf("Welcome %s to T2203M\n", Kien);
	return 0;
}
